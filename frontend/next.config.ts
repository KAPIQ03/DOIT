/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',

    basePath: '/~s51672/PSI/Projekt/frontend',

    images: {
        unoptimized: true,
    },
};

module.exports = nextConfig;